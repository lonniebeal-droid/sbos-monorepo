# Secret used to sign session JWTs. Generate with: openssl rand -base64 48
AUTH_SECRET=

# Base URL of the SBOS REST API (Phase 2). Used by the web app to proxy auth
# and data requests once the NestJS backend is wired in.
NEXT_PUBLIC_API_URL=http://localhost:4000
