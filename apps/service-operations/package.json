{
  "name": "@sbos/service-operations",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@sbos/core": "workspace:*"
  },
  "devDependencies": {
    "@sbos/tsconfig": "workspace:*",
    "@types/node": "^22.10.5",
    "typescript": "^7.0.2"
  }
}
